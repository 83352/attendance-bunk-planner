export default function Loading() {
  return <main className="mx-auto flex min-h-screen w-full max-w-[680px] items-center justify-center px-5 py-10 font-term text-[12px] uppercase text-muted">Loading schedule...</main>;
}